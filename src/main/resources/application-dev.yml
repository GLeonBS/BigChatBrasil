spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/big_chat_db
  config:
    activate:
      on-profile: dev
  liquibase:
    contexts: dev
  jpa:
    show-sql: true

security:
  token:
    secret:
      cliente: "cliente_secret_dev"
